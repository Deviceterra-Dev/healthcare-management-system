responses:
  200:
    description: "MFA setting updated successfully"
  404:
    description: "User not found"
parameters:
  - name: body
    in: body
    required: True
    schema:
      type: object
      properties:
        mfa_enabled:
          type: boolean
          example: true
      required:
        - mfa_enabled
security:
  - Bearer: []
