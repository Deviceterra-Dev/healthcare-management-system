responses:
  200:
    description: "Login successful"
    schema:
      type: object
      properties:
        token:
          type: string
        refresh_token:
          type: string
  401:
    description: "Invalid credentials"
parameters:
  - name: body
    in: body
    required: True
    schema:
      type: object
      properties:
        email:
          type: string
          example: "johndoe@example.com"
        password:
          type: string
          example: "Password123!"
      required:
        - email
        - password
